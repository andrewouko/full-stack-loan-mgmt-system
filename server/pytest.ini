[pytest]
minversion = 7.0
testpaths = tests
python_files = test_*.py
addopts = --maxfail=1 --strict-markers --cov=server --cov-report=term-missing
markers =
    integration: integration tests that may use network or DB
    unit: fast unit tests